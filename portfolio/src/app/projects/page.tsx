import { Repo } from "../types/Repo";

export const revalidate = 3600;
interface RepoProps {
    repo: Repo; 
}

export default function ProjectsPage() {

    return (
        <>
        
        </>
    )
}